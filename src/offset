#! .desc:
# Offset arguments by N for a function call
#! .params:
# <[$1]> - N
# <$@> - map()
#! .rc:
# (0) success
# (*) error
#! .ec:
# (255) input error
#! .desc.ext:
# If the first character of $1 is `-` or value is `0`, no offset will occur.
#
# The offset shall not exceed the arguments.
#
# For more information, refer to the documentation of map().
#.
offset() {
    _a="$1" && \
    _a="${_a%%\-*}" && \
    _a="${_a:-0}" && \
    assert -whole-n "$_a" || exit 255

    _a="$1" && \
    _a="${_a%%\-*}" && \
    _a="${_a:-0}" && \
    [ "$_a" != 0 ] || { shift && map "$@" && return 0 || return "$?"; }

    [ "$1" -le "$2" ] || exit 255

    _a="$1" && \
    _b="$2" && \
    shift "$((_a + 2))" && \
    set -- "$((_b - _a))" "$@" && \
    map "$@"
}
