#! .desc:
# Count all visible objects in a directory
#! .params:
# <"$1"> - directory path
# ["$2"] - suffix
#! .gives:
# (0) <$_count> - integer
#! .rc:
# (0) success
# (*) error
# (255) bad usage
#! .desc.ext:
# For more information, refer to:
# > "POSIX.1-2024, Volume: Shell & Utilities, Section: Shell Command Language,
#   Subsection: Pathname Expansion".
# > "POSIX.1-2024, Volume: Shell & Utilities, Section: Shell Command Language,
#   Subsection: Patterns Used for Filename Expansion".
#.
count_obj() {
    [ -d "$1" ] || return 255

    # Use pathname expansion with a glob pattern to match visible files
    # and directories in $1 (directory), optionally with a suffix ($2).
    set -- "$1"/*"$2" || return "$?"

    _count=$# && [ -e "$1" ] || _count=0
}
