#! .desc:
# Process each line of a file using a specified function
#! .params:
# <"$1"> - absolute file path
# <$2> - function name
#! .rc:
# (0) success
# (*) error
# (255) bad input/usage
#.
fline_parse() {
    { while IFS= read -r _line; do
        "$2" "$_line"
    done < "$1"; } || return 255; "$2" "$_line"
}
