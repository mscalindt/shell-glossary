#! .desc:
# Lexically normalize a path into an assertable canonical path
#! .params:
# [$1] - path
#! .desc.ext:
# For more information, refer to the documentation of path_spec_static().
#.
path_spec() {
    path_spec_static -abcklmr "$@"  # -> $_path
}
