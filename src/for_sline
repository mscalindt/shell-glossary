#! .desc:
# Process each populated string line with a function
#! .params:
# <$1> - string
# <$2> - function name
# [$3]+ - function argument
#! .rc:
# (0) success
# (*) error
#! .desc.ext:
# The function can access the string line using $_line. Additionally, $_line
# will be passed as the last argument.
#
# For more information, refer to:
# > "POSIX.1-2024, Volume: Shell & Utilities, Section: Shell Command Language,
#   Subsection: Here-Document".
#.
for_sline() {
    _str="$1"; shift; while IFS= read -r _line; do
        [ "$_line" ] || continue
        "$@" "$_line" || return "$?"
    done \
<<EOF
$_str
EOF
}
